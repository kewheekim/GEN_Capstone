<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-feature android:name="android.hardware.type.watch" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.Rally">
        <uses-library
            android:name="com.google.android.wearable"
            android:required="true" />

        <!--
               Set to true if your app is Standalone, that is, it does not require the handheld
               app to run.
        -->
        <meta-data
            android:name="com.google.android.wearable.standalone"
            android:value="true" />

        <activity
            android:name=".presentation.ScoreSwipeActivity"
            android:exported="false"
            android:taskAffinity=""
            android:theme="@style/ScoreActivityTheme.Starting" />

        <activity
            android:name=".presentation.StartActivity"
            android:exported="true" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".presentation.ResultSwipeActivity"
            android:exported="false"
            android:taskAffinity=""
            android:theme="@style/ScoreActivityTheme.Starting" />

        <service
            android:name=".datalayer.WatchDataLayerListener"
            android:exported="true">
            <!-- 메시지: 폰 -> 워치 (/rally/ack) -->
            <intent-filter>
                <action android:name="com.google.android.gms.wearable.MESSAGE_RECEIVED"/>
                <data android:scheme="wear" android:host="*" android:path="/rally/ack"/>
            </intent-filter>
            <!-- 메시지: 폰 -> 워치 경기 세팅(/rally/match_setup) -->
            <intent-filter>
                <action android:name="com.google.android.gms.wearable.MESSAGE_RECEIVED"/>
                <data android:scheme="wear" android:host="*" android:path="/rally/match_setup"/>
            </intent-filter>
            <!-- 폰으로부터 스냅샷(data Item) (/rally/snapshot) 수신 -->
            <intent-filter>
                <action android:name="com.google.android.gms.wearable.DATA_CHANGED"/>
                <data android:scheme="wear" android:host="*" android:path="/rally/snapshot"/>
            </intent-filter>
        </service>
        <meta-data
            android:name="com.google.android.wearable.capability"
            android:resource="@xml/wear_capabilities_watch"/>
    </application>
</manifest>